import React, { useEffect, useState } from 'react'
import PropTypes from 'prop-types'

const useDonationFeedback = (amount) => {
  
    
}

useDonationFeedback.propTypes = {
    amount:PropTypes.number
}

export default useDonationFeedback